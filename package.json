{
    "scripts": {
        "prod": "json-server db.json &",
        "posttest": "pkill -f json-server"
    },
    "dependencies": {
        "forever-monitor": "^1.7.1",
        "json-server": "^0.15.1"
    }
}
